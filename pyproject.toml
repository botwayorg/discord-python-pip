[tool.poetry]
name = "{{.BotName}}"
description = "My Awesome Discord (Python) Bot"

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
